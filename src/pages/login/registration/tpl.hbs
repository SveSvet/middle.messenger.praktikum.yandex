<main class="container">
  <div class="container__card">
    <div class="registration-page">
      {{{title}}}
      <form action="" class="registration-page__form" onsubmit="">
        <div class="form__inputs">
          {{{children}}}
        </div>
        <div class="card__buttons">
          <div class="card__buttons card__buttons--registration">
            {{{registrationButton}}}
          </div>
          {{{loginButton}}}
        </div>
      </form>
    </div>
  </div>
</div>
