<main class="container">
  <div class="container__card">
    <div class="login-page">
      {{{title}}}
      <form action="" class="login-page__form" onsubmit="">
        <div class="form__inputs">
          {{{children}}}
        </div>
        <div class="card__buttons">
          <div class="card__buttons card__buttons--login">
            {{{registrationButton}}}
          </div>
          {{{loginButton}}}
        </div>
      </form>
    </div>
  </div>
</main>
